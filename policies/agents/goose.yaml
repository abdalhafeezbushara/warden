name: agent:goose
description: Warden baseline for Goose. Block's open-source agent; provider-dependent.

filesystem:
  read:
    - .//**
    - ~/.gitconfig
    - ~/.config/git/**
    - ~/.goose/**
    - ~/.config/goose/**
  write:
    - .//**
    - /tmp/**
    - /private/tmp/**
    - ~/.goose/**
    - ~/.config/goose/**
  deny:
    - ~/.ssh/**
    - ~/.aws/**
    - ~/.gnupg/**
    - ~/.config/gcloud/**
    - ~/.kube/**
    - ~/.docker/config.json
    - ~/.netrc
    - ~/.npmrc
    - ~/.pypirc
    - ~/.git-credentials
    - **/.env
    - **/.env.*
    - ~/Library/Keychains/**

network:
  allow:
    - *.github.com
    - *.githubusercontent.com
    - api.anthropic.com
    - api.openai.com
    - codeload.github.com
    - crates.io
    - files.pythonhosted.org
    - get.pnpm.io
    - github.com
    - proxy.golang.org
    - pypi.org
    - registry.npmjs.org
    - registry.yarnpkg.com
    - static.crates.io
  deny: []
  deny_all_other: true

process:
  deny:
    - ssh
    - scp
    - aws
    - gcloud
    - kubectl

on_violation: block+receipt
